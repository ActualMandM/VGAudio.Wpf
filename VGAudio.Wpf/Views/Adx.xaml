<UserControl x:Class="VGAudio.Wpf.Views.Adx"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:VGAudio.Wpf.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             DataContext="{Binding MainWindow, Source={StaticResource Locator}}">
    <Grid>
        <Grid.Resources>
            <local:TrackTypeEnumToBoolConverter x:Key="TrackTypeConverter" />
            <local:SeekTableTypeEnumToBoolConverter x:Key="SeekTableTypeConverter" />
        </Grid.Resources>
        <Grid HorizontalAlignment="Stretch">
            <Grid.RowDefinitions>
                <RowDefinition/>
            </Grid.RowDefinitions>
            <DockPanel Margin="0,10,0,0">
                <TextBlock Text="Key code: " VerticalAlignment="Center" Grid.Row="2" Grid.Column="0"/>
                <TextBox Text="{Binding AdxConfiguration.EncryptionKey, Mode=TwoWay}" Grid.Row="2" Grid.Column="2"/>
            </DockPanel>
        </Grid>
    </Grid>
</UserControl>
